📋 SOLUÇÃO ÚNICA – BAILEYS NO REPLIT COM QR E PROCESSAMENTO DE ÁUDIO

❌ PROBLEMA ATUAL:
- QR Code não aparece na interface web (apenas no console).
- Replit fica em loop e não gera o QR para o cliente escanear.
- Primeira resposta em áudio não é processada (não salva nem transcreve).

✅ PASSOS ÚNICOS A IMPLEMENTAR:
1. Parar de usar qrcode-terminal; usar qrcode.toDataURL(qr) para gerar base64.
2. Criar rota GET `/qr` que sirva `<img src="${base64}" />` no navegador.
3. Manter a pasta `auth_info` persistente com useMultiFileAuthState('auth_info') sem reiniciar o bot.
4. Em messages.upsert, detectar `msg.message.audioMessage`, baixar o buffer via sock.downloadMediaMessage(msg).
5. Salvar buffer no Firebase Storage e obter URL.
6. Enviar buffer à API Whisper, extrair `resp.data.text`.
7. Salvar no Firestore: `{ nome, numero, perguntaAtual, respostaAudioUrl, respostaTexto, numeroDaPergunta }`.
8. Após salvar, verificar próxima pergunta: se existir, enviar texto + áudio TTS; senão, enviar mensagem de encerramento.

🔧 DEPENDÊNCIAS (rodar uma única vez):
npm install express @whiskeysockets/baileys qrcode axios firebase-admin dotenv

🗄️ VARIÁVEIS DE AMBIENTE NECESSÁRIAS:
FIREBASE_SERVICE_ACCOUNT (JSON)
FIREBASE_STORAGE_BUCKET
OPENAI_API_KEY
VAGA_ID